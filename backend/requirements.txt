fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                    # pin: passlib 1.7.4 breaks with bcrypt >= 4.1
pydantic-settings==2.1.0
python-dotenv==1.0.0
httpx==0.26.0
groq==0.4.2
PyMuPDF==1.23.8
pdfplumber==0.10.3
celery[redis]==5.3.6
redis==5.0.1
imap-tools==1.5.0
reportlab==4.0.9
jinja2==3.1.3
playwright==1.41.1
python-dateutil==2.8.2
requests==2.31.0
duckduckgo-search==6.3.7
lxml==5.3.0

# ── Auth additions (Phase 1) ─────────────────────────────────────────
email-validator==2.1.0          # pydantic EmailStr support
itsdangerous==2.1.2              # signed tokens (used by jose internally)

# (Billing/payments removed — open-source edition is unlimited & free.)

# ── Security — Rate Limiting ─────────────────────────────────────────
# Custom Redis-based rate limiter in auth/rate_limit.py (no extra package needed)
# Uses the redis package already installed above

# ── Phase 2a — Domain verification + DNS ─────────────────────────────
dnspython==2.6.1                 # DNS TXT record verification for domain ownership
